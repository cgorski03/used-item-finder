{
  "name": "used-item-finder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter web dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "db:push": "pnpm --filter packages/db drizzle-kit push"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/node": "^24.7.2"
  }
}
